name: rbp-trace-first-layer-rbp_trace
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - pyhmmer
  - pip
  - pip:
      - torch
      - transformers
